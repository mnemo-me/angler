<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="@color/black"
    android:paddingBottom="10dp"
    android:paddingTop="12dp">

    <Button
        android:id="@+id/add_tracks"
        android:layout_width="0dp"
        android:layout_height="36dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:paddingEnd="@dimen/default_margin"
        android:paddingStart="@dimen/default_margin"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="10dp"
        android:text="@string/add_tracks_to_playlist"
        android:textColor="@color/selected_grey_white"
        app:layout_constraintEnd_toStartOf="@id/manage_tracks"
        app:layout_constraintStart_toStartOf="parent" />

    <Button
        android:id="@+id/manage_tracks"
        android:layout_width="0dp"
        android:layout_height="36dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:paddingEnd="@dimen/default_margin"
        android:paddingStart="@dimen/default_margin"
        android:layout_marginStart="10dp"
        android:layout_marginEnd="24dp"
        android:text="@string/manage_tracks"
        android:textColor="@color/selected_grey_white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/add_tracks" />

</androidx.constraintlayout.widget.ConstraintLayout>